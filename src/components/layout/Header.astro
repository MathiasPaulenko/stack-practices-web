---
import { SITE, MAIN_NAV, type Locale } from '../../config/site';

export interface Props {
  locale?: Locale;
}

const { locale = SITE.defaultLocale } = Astro.props;
const prefix = locale === 'es' ? '/es' : '';
const navPrefix = locale === 'es' ? '/es/' : '/';
const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 border-b border-slate-700 bg-slate-900/95 backdrop-blur">
  <div class="mx-auto flex h-16 max-w-6xl items-center justify-between gap-4 px-4 sm:px-6">
    <a href={`${prefix}/`} class="flex items-center gap-2 font-bold text-white">
      <span class="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-brand-500">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 32 32" aria-hidden="true">
          <path d="M11 6.5C9 6.5 8 7.8 8 9.5v1.2c0 1-.5 1.8-1.5 2.2 1 .4 1.5 1.2 1.5 2.2v1.2c0 1.7 1 3 3 3" fill="none" stroke="#fff" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" opacity="0.95"/>
          <path d="M21 6.5c2 0 3 1.3 3 3v1.2c0 1 .5 1.8 1.5 2.2-1 .4-1.5 1.2-1.5 2.2v1.2c0 1.7-1 3-3 3" fill="none" stroke="#fff" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" opacity="0.95"/>
          <rect x="12" y="14" width="8" height="2.5" rx="1.25" fill="#fff" opacity="0.55"/>
          <rect x="11" y="18.5" width="10" height="2.5" rx="1.25" fill="#fff" opacity="0.75"/>
          <rect x="10" y="23" width="12" height="2.5" rx="1.25" fill="#fff"/>
        </svg>
      </span>
      <span class="text-lg">{SITE.name}</span>
    </a>

    <nav aria-label="Main navigation" class="hidden items-center gap-1 md:flex">
      {
        MAIN_NAV.map((item) => {
          const href = `${prefix}${item.href}`;
          const isActive = currentPath.startsWith(href);
          return (
            <a
              href={href}
              class:list={[
                'rounded-md px-3 py-2 text-sm font-medium transition-colors',
                isActive
                  ? 'bg-brand-500/20 text-brand-300'
                  : 'text-slate-300 hover:bg-slate-800 hover:text-white',
              ]}
              aria-current={isActive ? 'page' : undefined}
            >
              {item.label}
            </a>
          );
        })
      }
    </nav>

    <div class="flex items-center gap-2">
      <a
        href={`${prefix}/search/`}
        class="rounded-md p-2 text-slate-300 hover:bg-slate-800 hover:text-white"
        aria-label="Search"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path></svg>
      </a>
      <a
        href={locale === 'es' ? currentPath.replace(/^\/es/, '') || '/' : `/es${currentPath.endsWith('/') ? currentPath : `${currentPath}/`}`}
        class="rounded-md border border-slate-600 px-2.5 py-1.5 text-xs font-semibold text-slate-300 hover:bg-slate-800 hover:text-white"
        aria-label="Switch language"
      >
        {locale === 'es' ? 'EN' : 'ES'}
      </a>
    </div>
  </div>
</header>
