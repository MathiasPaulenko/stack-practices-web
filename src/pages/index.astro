---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE, CONTENT_TYPES, TECHNOLOGIES } from '../config/site';
import { webPage } from '../lib/schema';
import { getCollection } from 'astro:content';
import { isSpanish } from '../lib/content';

const [recipes, patterns, docs, guides] = await Promise.all([
  getCollection('recipes', ({ id }) => !isSpanish(id)),
  getCollection('patterns', ({ id }) => !isSpanish(id)),
  getCollection('docs', ({ id }) => !isSpanish(id)),
  getCollection('guides', ({ id }) => !isSpanish(id)),
]);

const recipesCount = recipes.length;
const patternsCount = patterns.length;
const docsCount = docs.length;
const guidesCount = guides.length;
const topics = new Set<string>();
for (const col of [recipes, patterns, docs, guides]) {
  for (const entry of col) {
    entry.data.topics.forEach((t: string) => topics.add(t));
  }
}
const topicsCount = topics.size;

const jsonLd = [
  webPage({
    name: SITE.title,
    description: SITE.description,
    url: '/',
    locale: 'en',
  }),
  {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: SITE.name,
    url: SITE.url,
    potentialAction: {
      '@type': 'SearchAction',
      target: {
        '@type': 'EntryPoint',
        urlTemplate: `${SITE.url}/search?q={search_term_string}`,
      },
      'query-input': 'required name=search_term_string',
    },
  },
];

const concepts = [
  { label: 'API Design', slug: 'api' },
  { label: 'Authentication', slug: 'authentication' },
  { label: 'Architecture', slug: 'architecture' },
  { label: 'Performance', slug: 'performance' },
  { label: 'Testing', slug: 'testing' },
  { label: 'Security', slug: 'security' },
];
---

<BaseLayout
  title={SITE.title}
  description={SITE.description}
  path="/"
  ogType="website"
  keywords={['code recipes', 'design patterns', 'software engineering', 'developer documentation']}
  jsonLd={jsonLd}
>
  <!-- Hero -->
  <section class="hero-animated-bg bg-slate-900">
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-dot" aria-hidden="true"></span>
    <span class="hero-code-symbol" aria-hidden="true">{ }</span>
    <span class="hero-code-symbol" aria-hidden="true">&lt;/&gt;</span>
    <span class="hero-code-symbol" aria-hidden="true">//</span>
    <span class="hero-code-symbol" aria-hidden="true">[ ]</span>
    <span class="hero-code-symbol" aria-hidden="true">&&</span>
    <span class="hero-code-symbol" aria-hidden="true">;;</span>
    <div class="relative z-10 mx-auto max-w-6xl px-4 py-20 text-center sm:px-6 sm:py-28">
      <h1 class="mx-auto max-w-3xl text-4xl font-extrabold tracking-tight text-white sm:text-5xl">
        Practical software engineering knowledge, ready to reuse
      </h1>
      <p class="mx-auto mt-6 max-w-2xl text-lg text-slate-300">
        Find real-world code recipes, design patterns, and reusable documentation across
        Python, Java, JavaScript, SQL, and more.
      </p>
      <div class="relative z-10 mx-auto mt-10 flex flex-wrap justify-center gap-8 sm:gap-12">
        <div class="text-center">
          <div class="text-2xl font-bold text-white">{recipesCount}</div>
          <div class="mt-1 text-xs font-medium uppercase tracking-wider text-slate-400">Recipes</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-white">{patternsCount}</div>
          <div class="mt-1 text-xs font-medium uppercase tracking-wider text-slate-400">Patterns</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-white">{docsCount}</div>
          <div class="mt-1 text-xs font-medium uppercase tracking-wider text-slate-400">Docs</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-white">{guidesCount}</div>
          <div class="mt-1 text-xs font-medium uppercase tracking-wider text-slate-400">Guides</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-white">{topicsCount}</div>
          <div class="mt-1 text-xs font-medium uppercase tracking-wider text-slate-400">Topics</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Browse by Type -->
  <section class="mx-auto max-w-6xl px-4 py-16 sm:px-6">
    <h2 class="text-2xl font-bold text-slate-900">Browse by type</h2>
    <p class="mt-2 text-slate-600">Choose the kind of resource you need for your project.</p>
    <div class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
      {CONTENT_TYPES.map((type) => {
        const count = type.slug === 'recipes' ? recipesCount : type.slug === 'patterns' ? patternsCount : type.slug === 'docs' ? docsCount : guidesCount;
        const icon = type.slug === 'recipes' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 2v2"/><path d="M15 2v2"/><path d="M12 2v8"/><path d="M8 4h8a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"/></svg>
        ) : type.slug === 'patterns' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect width="7" height="7" x="3" y="3" rx="1"/><rect width="7" height="7" x="14" y="3" rx="1"/><rect width="7" height="7" x="14" y="14" rx="1"/><rect width="7" height="7" x="3" y="14" rx="1"/></svg>
        ) : type.slug === 'docs' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><polyline points="14 2 14 8 20 8"/><line x1="16" x2="8" y1="13" y2="13"/><line x1="16" x2="8" y1="17" y2="17"/></svg>
        ) : (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>
        );
        return (
          <a
            href={`/${type.slug}`}
            class="group relative flex flex-col rounded-xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:border-brand-400 hover:shadow-lg"
          >
            <div class="flex items-start gap-3">
              <span class="grid h-10 w-10 shrink-0 place-items-center rounded-lg bg-slate-800 text-white">{icon}</span>
              <div class="flex-1 min-w-0">
                <div class="flex items-center justify-between gap-2">
                  <h3 class="text-base font-semibold text-slate-900 group-hover:text-brand-600">{type.label}</h3>
                  <span class="rounded-full bg-slate-100 px-2.5 py-0.5 text-xs font-semibold text-slate-600">{count}</span>
                </div>
                <p class="mt-1 text-sm text-slate-500">{type.description}</p>
              </div>
            </div>
            <div class="mt-3 flex items-center text-sm font-medium text-brand-600">
              Explore
              <svg class="ml-1.5 h-4 w-4 transition group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
            </div>
          </a>
        );
      })}
    </div>
  </section>

  <!-- Browse by Technology -->
  <section class="border-t border-slate-200 bg-slate-50">
    <div class="mx-auto max-w-6xl px-4 py-16 sm:px-6">
      <h2 class="text-2xl font-bold text-slate-900">Browse by technology</h2>
      <p class="mt-2 text-slate-600">Find resources for your preferred programming language or tool.</p>
      <div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
        {TECHNOLOGIES.map((tech) => {
          const icon = tech === 'python' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2c-2.5 0-4 1.5-4 4v2H7c-2.5 0-4 1.5-4 4s1.5 4 4 4h3v-2c0-2.5 1.5-4 4-4h2V6c0-2.5-1.5-4-4-4z"/><path d="M12 22c2.5 0 4-1.5 4-4v-2h1c2.5 0 4-1.5 4-4s-1.5-4-4-4h-3v2c0 2.5-1.5 4-4 4H7v2c0 2.5 1.5 4 4 4z"/></svg>
          ) : tech === 'java' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9c0 3 2 5 6 5s6-2 6-5"/><path d="M12 14v4"/><path d="M8 22h8"/></svg>
          ) : tech === 'javascript' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M8 17v-5"/><path d="M12 17v-1a2 2 0 0 1 2-2h0"/></svg>
          ) : tech === 'typescript' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M8 17v-5"/><path d="M12 17v-1a2 2 0 0 1 2-2h0"/><line x1="16" x2="18" y1="12" y2="12"/></svg>
          ) : tech === 'sql' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg>
          ) : tech === 'bash' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M8 10l3 3-3 3"/><path d="M13 16h4"/></svg>
          ) : tech === 'docker' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="4" y="10" width="4" height="6"/><rect x="10" y="10" width="4" height="6"/><rect x="16" y="10" width="4" height="6"/><rect x="7" y="4" width="4" height="6"/><rect x="13" y="4" width="4" height="6"/><path d="M2 16h20"/></svg>
          ) : tech === 'git' ? (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="6" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="12" r="3"/><path d="M6 9v6"/><path d="M9 6h3a3 3 0 0 1 3 3v3"/></svg>
          ) : (
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18"/><path d="M9 21V9"/></svg>
          );
          return (
            <a href={`/topics?tech=${tech}`} class="flex items-center gap-4 rounded-xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-brand-400 hover:shadow-md">
              <span class="grid h-10 w-10 shrink-0 place-items-center rounded-lg bg-slate-800 text-white">{icon}</span>
              <div>
                <h3 class="text-base font-semibold capitalize text-slate-900">{tech}</h3>
                <p class="mt-0.5 text-xs text-slate-500">Code examples & guides</p>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- Browse by Concept -->
  <section class="mx-auto max-w-6xl px-4 py-16 sm:px-6">
    <h2 class="text-2xl font-bold text-slate-900">Browse by concept</h2>
    <p class="mt-2 text-slate-600">Dive into core software engineering themes.</p>
    <div class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
      {concepts.map((concept) => {
        const s = concept.slug;
        const icon = s === 'api' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
        ) : s === 'authentication' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        ) : s === 'architecture' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><line x1="3" x2="21" y1="9" y2="9"/><line x1="9" x2="9" y1="21" y2="9"/></svg>
        ) : s === 'performance' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
        ) : s === 'testing' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
        ) : s === 'security' ? (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        ) : (
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" x2="12" y1="8" y2="12"/><line x1="12" x2="16" y1="12" y2="12"/></svg>
        );
        const desc = s === 'api' ? 'REST, GraphQL, and API design patterns for robust integrations.' : s === 'authentication' ? 'JWT, OAuth, session management, and secure auth flows.' : s === 'architecture' ? 'System design, microservices, and scalable patterns.' : s === 'performance' ? 'Optimization strategies for speed and efficiency.' : s === 'testing' ? 'Unit, integration, and end-to-end testing strategies.' : 'Secure coding practices and vulnerability prevention.';
        return (
          <a href={`/topics/${concept.slug}`} class="group relative flex flex-col rounded-xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:border-brand-400 hover:shadow-lg">
            <div class="flex items-start gap-3">
              <span class="grid h-10 w-10 shrink-0 place-items-center rounded-lg bg-slate-800 text-white">{icon}</span>
              <div class="flex-1 min-w-0">
                <h3 class="text-base font-semibold text-slate-900 group-hover:text-brand-600">{concept.label}</h3>
                <p class="mt-1 text-sm text-slate-500">{desc}</p>
              </div>
            </div>
            <div class="mt-3 flex items-center text-sm font-medium text-brand-600">
              Explore
              <svg class="ml-1.5 h-4 w-4 transition group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
            </div>
          </a>
        );
      })}
    </div>
  </section>

  <!-- Latest Recipes -->
  <section class="border-t border-slate-200 bg-slate-50">
    <div class="mx-auto max-w-6xl px-4 py-16 sm:px-6">
      <div class="flex items-center justify-between">
        <div>
          <h2 class="text-2xl font-bold text-slate-900">Latest recipes</h2>
          <p class="mt-2 text-slate-600">Fresh code solutions added to the knowledge base.</p>
        </div>
        <a href="/recipes" class="hidden text-sm font-semibold text-brand-600 hover:text-brand-700 sm:flex sm:items-center">
          View all
          <svg class="ml-1.5 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
        </a>
      </div>
      <div class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
        {recipes.slice(0, 3).map((recipe) => (
          <a href={`/recipes/${recipe.data.slug}`} class="group flex flex-col rounded-xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-brand-400 hover:shadow-lg">
            <div class="flex items-center gap-2">
              <span class="rounded-full bg-slate-100 px-2.5 py-0.5 text-xs font-semibold uppercase text-slate-600">{recipe.data.difficulty}</span>
              <span class="text-xs text-slate-400">{recipe.data.topics[0]?.replace(/-/g, ' ')}</span>
            </div>
            <h3 class="mt-3 text-base font-semibold text-slate-900 group-hover:text-brand-600">{recipe.data.title}</h3>
            <p class="mt-1 flex-1 text-sm text-slate-500">{recipe.data.description}</p>
            <div class="mt-4 flex flex-wrap gap-2">
              {recipe.data.tags.slice(0, 3).map((tag: string) => (
                <span class="rounded-full border border-slate-200 bg-white px-2 py-0.5 text-xs text-slate-600">{tag}</span>
              ))}
            </div>
          </a>
        ))}
      </div>
      <div class="mt-6 text-center sm:hidden">
        <a href="/recipes" class="text-sm font-semibold text-brand-600 hover:text-brand-700">View all recipes →</a>
      </div>
    </div>
  </section>
</BaseLayout>
