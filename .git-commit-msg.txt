feat: add GA4 content_type custom dimension to all pageviews

P1.4 - Custom dimension contentType in GA4:
- BaseLayout.astro: new contentType prop (default 'page'), inline
  dataLayer push before analytics.js loads
- analytics.js: reads content_type from dataLayer and sends it with
  gtag('config', ..., { content_type }) so GA4 receives it on every
  pageview
- RecipeArticle, ListingPage, AuthorPage: pass contentType to BaseLayout
- Home (EN+ES): contentType="home"
- Topics (EN+ES): contentType="topic"
- Tags (EN+ES, index + [tag]): contentType="tag"
- Authors (EN+ES): contentType="author"
- Search (EN+ES): contentType="search"
- Static pages (about, privacy, terms, etc.): default "page"

Content types sent to GA4: recipes, patterns, guides, docs, home, topic,
tag, author, search, page.

GA4 UI config required (manual):
- Admin > Custom definitions > Create custom dimension
- Name: Content Type, Scope: Event, Parameter: content_type

Validation:
- npm run check: 0 errors, 0 warnings, 0 hints
- npm run build: 3258 pages OK
- content_type verified in HTML output for home, recipe, guide, about,
  topic
- SRI hash regenerated correctly

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
